"use client";

import { RiPlayCircleLine } from "react-icons/ri";

const IconRiPlayCircleLine = ({ size }: { size: number }) => {
  return <RiPlayCircleLine size={size} />;
};

export default IconRiPlayCircleLine;
